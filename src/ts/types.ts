export interface GUISettings {
    sRange: number;
}

export interface PointCoords {
    x: number;
    y: number;
    z: number;
}

export interface FieldArea {
    width: number;
    height: number;
    depth: number;
}

export type a1DArr = number[];
export type a2DArr = a1DArr[];
export type a3DArr = a2DArr[];
